# WeatherApp
I have coded my own weather app with OpenWeather API. It tells you the temperature, type of sky and wind speed of a city. It uses Python, JavaScript, HTML and CSS to run. Python uses the API to fetch the data, while JavaScript is the one which fills in the results for your specific city on-screen.
